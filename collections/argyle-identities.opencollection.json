{
  "opencollection": "1.0.0",
  "info": {
    "name": "Identities API",
    "version": "2.0"
  },
  "request": {
    "auth": {
      "type": "basic",
      "username": "{{username}}",
      "password": "{{password}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Retrieve an identity",
        "type": "http"
      },
      "http": {
        "method": "GET",
        "url": "{{baseUrl}}/v2/identities/:id",
        "params": [
          {
            "name": "id",
            "value": "",
            "type": "path",
            "description": "ID of the identity object to be retrieved."
          }
        ]
      },
      "docs": "Retrieves an identity object."
    },
    {
      "info": {
        "name": "List all identities",
        "type": "http"
      },
      "http": {
        "method": "GET",
        "url": "{{baseUrl}}/v2/identities",
        "params": [
          {
            "name": "account",
            "value": "",
            "type": "query",
            "description": "Filter by account ID."
          },
          {
            "name": "user",
            "value": "",
            "type": "query",
            "description": "Filter by user ID."
          },
          {
            "name": "employment",
            "value": "",
            "type": "query",
            "description": "Filter by employment ID."
          },
          {
            "name": "limit",
            "value": "",
            "type": "query",
            "description": "Number of identity objects returned per page. Default: 10. Maximum: 200.\n"
          },
          {
            "name": "cursor",
            "value": "",
            "type": "query",
            "description": "The URL returned in `next` or `previous` used to retrieve another [page](/api-guide/overview#pagination) of results."
          }
        ]
      },
      "docs": "Returns a paginated list of all identity objects."
    }
  ],
  "bundled": true
}