{
  "opencollection": "1.0.0",
  "info": {
    "name": "Vehicles API",
    "version": "2.0"
  },
  "request": {
    "auth": {
      "type": "basic",
      "username": "{{username}}",
      "password": "{{password}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Retrieve a vehicle",
        "type": "http"
      },
      "http": {
        "method": "GET",
        "url": "{{baseUrl}}/v2/vehicles/:id",
        "params": [
          {
            "name": "id",
            "value": "",
            "type": "path",
            "description": "ID of the vehicle object to be retrieved."
          }
        ]
      },
      "docs": "Retrieves a vehicle object."
    },
    {
      "info": {
        "name": "List all vehicles",
        "type": "http"
      },
      "http": {
        "method": "GET",
        "url": "{{baseUrl}}/v2/vehicles",
        "params": [
          {
            "name": "account",
            "value": "",
            "type": "query",
            "description": "Filter by account ID."
          },
          {
            "name": "user",
            "value": "",
            "type": "query",
            "description": "Filter by user ID."
          },
          {
            "name": "limit",
            "value": "",
            "type": "query",
            "description": "Number of vehicle objects returned [per page](/api-guide/overview#pagination). Default: 10. Maximum: 200.\n"
          },
          {
            "name": "cursor",
            "value": "",
            "type": "query",
            "description": "The URL returned in `next` or `previous` used to retrieve another [page](/api-guide/overview#pagination) of results."
          }
        ]
      },
      "docs": "Returns a paginated list of all vehicle objects."
    }
  ],
  "bundled": true
}